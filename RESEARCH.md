# 混元 3D 网页工作台功能调研

观察日期：**2026-09-08**。本文整理开发时在已登录的[官方网页工作台](https://3d.hunyuan.tencent.com/)中观察和验证的功能。网页可能更新，账号可用性和额度也可能不同；本文不是腾讯官方功能或计费承诺。

工具针对 **Studio 网页流程**，使用常驻 Chrome 操作可见页面。没有通过腾讯云 API 提交任务，也没有依据云 API 文档推定网页拥有相同参数或共享额度。

## Studio 功能与实现边界

| 入口 | 观察到的输入与参数 | 工具状态 | 尚需验证或人工参与 |
|---|---|---|---|
| [概念设计](https://3d.hunyuan.tencent.com/studio/creation/concept) | 文字、人物参考图、多视图、A-pose；文字最多 150 字；图生多视图标明限人物 | 代表性表单准备通过 | 概念图实际生成、多视图全部输出和下载 |
| [几何生成](https://3d.hunyuan.tencent.com/studio/creation/geo) | 单图、多视图；V3.1；50k / 500k / 1M / 1.5M | 三视图几何生成、监控、下载通过；单图代表性表单通过 | 各面数、题材和其他模式的生成质量不能由少量样例保证 |
| [组件拆分](https://3d.hunyuan.tencent.com/studio/creation/comp) | 本地 FBX / GLB / OBJ / STL；预分割入口 | 模型上传和表单准备通过 | 预分割后选择与调整部件、最终确认和分件导出 |
| [低模生成](https://3d.hunyuan.tencent.com/studio/creation/poly) | 本地 FBX / GLB / OBJ；低模拓扑 V1.5；低/中/高；三角面/四边面 | 低档、四边面代表性表单通过 | 最终网格、动画拓扑适用性和产物下载 |
| [UV 展开](https://3d.hunyuan.tencent.com/studio/creation/uv) | 本地 FBX / GLB / OBJ；智能展开 UV | 代表性表单准备通过 | UV 岛、重叠、拉伸、接缝与最终产物 |
| [纹理绘制](https://3d.hunyuan.tencent.com/studio/creation/texture) | 模型 + 文字/单图/多图；神奇笔刷；文字最多 150 字 | 多视图纹理生成、自动下载 GLB、Blender 导入通过；文字代表性表单通过 | 其他模式产物、笔刷前提及区域和笔触操作 |
| [绑骨蒙皮](https://3d.hunyuan.tencent.com/studio/creation/rs) | 本地 FBX / GLB / OBJ；超过 50 万面不支持自动绑骨的页面提示 | 代表性模型上传和表单准备通过 | 真实人物的姿态、骨骼、蒙皮权重和关节变形 |
| [动画生成](https://3d.hunyuan.tencent.com/studio/creation/ae) | 角色选择器、走路/待机等动作模板；没有直接本地模型上传控件 | 可见角色与待机模板选择通过 | 实际动画生成、重定向和导出文件 |

表单验证只确认特定输入在当时页面能准备到可提交状态，**没有为表格中的所有功能消耗额度生成样品**。当前 CLI 的 `capabilities` 返回能力目录以及 `ui_preparation`、`generation_e2e` 等字段，可供调用方区分准备流程与完整生成验证。

## 输入预设

- 图片为 PNG / JPEG / WEBP，每边 128–4096 像素，单张不超过 10 MiB。
- 当时网页多视图接受 2–8 张；工具基于三视图生产约定，要求 **3–8 张且包含正面**。
- 工具检查图片哈希，拒绝把完全相同的图片作为不同方向。不同文件并不自动意味着视图一致或方向正确，仍需人工检查。
- 模型按具体功能接受 GLB / FBX / OBJ，组件拆分额外接受 STL。工具有本地大小和格式检查；这不是对官网所有文件规格的保证。
- 依赖外部材质文件的 OBJ 需先打包为 GLB。绑骨的 GLB 会检查声明三角面数，但不会据此判断人物形体是否适合自动绑骨。

概念设计的“图生多视图”限制为人物，不宜当成通用道具三视图生成器。道具可以先由其他图像工具提供一致视图，再交给几何生成。

## 成品识别、监控和下载

当前观察到的资产卡没有公开显示稳定的官网任务号。本地 `hy_...` 是工具自己的任务编号，不是腾讯服务端任务号。

1. 提交前保存已有资产卡。
2. 提交后只有唯一新增卡片时，绑定这个具体元素。
3. 排队或生成过程中持续检查绑定卡片，不按列表首项猜测。
4. 完成后保存可唯一匹配的缩略图身份，选中对应资产，再通过官方菜单导出。
5. 若生成中的浏览器上下文丢失且没有稳定身份，暂停监控，核对完成资产后显式恢复。

| 导出格式 | 实际验证 |
|---|---|
| GLB | 自制校准模型的传输导出通过；真实多视图纹理生成后的下载通过 |
| STL | 真实三视图几何结果下载通过 |
| FBX | 真实几何结果下载通过；曾遇到 Chrome 导出崩溃，恢复同一成品后成功 |
| USDZ / MP4 / GIF | 当前菜单入口已观察到；尚无对应产物的完整端到端验收 |
| 概念图与分件输出 | 需要专门实现和验收，不能从模型格式下载成功推定已覆盖 |

可用格式按选中的具体资产发现，不能假设每个功能都能导出全部格式。`auto` 优先选择适合该工作流的格式；下载失败不会触发重新生成。

## 效率来自哪里

固定网页步骤由一个本地 Playwright worker 连续执行；任务队列、状态和监控结果写入本地文件与 SQLite。模型只需准备参数、发起操作、读取阶段结果或处理异常。

这能降低重复浏览器交互与模型调用的开销，并通过任务去重和提交状态控制减少误重复生成。它不缩短服务端排队和计算时间，不增加网页额度，也不证明网页额度与腾讯云 API 额度互通。

监控需要本机及 worker 保持运行。登录、验证码、结果身份不清晰和模型质量判断都有明确的人工作业点。批量任务也不会自动把几何输出串到纹理、绑骨和动画；后续任务需要已就绪的输入。

## 未接入的网页入口

普通首页的图/文生 3D、全景图、世界重建尚未接入本版 Studio 任务流程。**世界生成明确排除**。有些首页入口与 Studio 使用相似名称，不能视为同一套表单和任务状态已被支持。

## 与 Blender / Unity 的边界

生成物进入 Blender 后仍需检查比例、朝向、拓扑、法线、材质和资源完整性。角色另需检查骨骼命名、权重、关节变形和动画。面向 VR 的资产还需根据设备预算处理面数、贴图、LOD、碰撞及材质复杂度。

当前工具提供原件归档和后台 Blender 导入脚本，不自动承诺资产已满足 Unity 或某款 VR 设备的实时性能要求。对开阔环境、建筑模组和需要精确尺寸的空间，仍适合在 Blender 中构建，再整合生成的角色和道具。
