# 更新记录

## 0.2.1 — 2026-09-08

面向可分发安装的首个 GitHub 版本，沿用 0.2.0 的网页工作台核心流程。

- 整理可安装的 Python 包，提供 `hunyuan-workbench` CLI 与 `hunyuan-workbench-mcp` stdio 入口。
- 将默认运行数据放入用户数据目录，支持 `--root` 和 `HUNYUAN_WORKBENCH_ROOT` 指定位置。
- 增补中文安装、任务准备、提交、监控、下载、恢复、Blender 和 MCP 文档。
- 提供通用三视图几何、纹理、低模、绑骨及批量参数示例。
- 从分发内容中排除登录配置、账号任务记录、生成资产、参考图和项目专用脚本。
- 明确区分表单已验证、真实生成已验证、导出格式已验证及尚未接入的功能。
- 修复全新数据目录下首次下载的元数据目录创建；探测辅助函数改用显式提供的模型。
- 新增 7 项目录与分发相关回归测试，27 项离线测试通过，stdio MCP 19 个工具检查通过。

此版本没有把尚待验收的功能标记为完整支持；真实官网生成验证沿用开发基线，打包不需要再次提交生成任务。

## 0.2.0 — 2026-09-08

本地生产验证版本。

- 常驻 Chrome worker 与本地文件队列，使用独立持久网页会话。
- 输入校验、复制和哈希去重，SQLite 任务账本及状态事件。
- 八个 Studio 功能的参数描述与代表性表单适配。
- 提交资格控制、具体资产卡绑定、后台监控和完成后自动下载。
- GLB / STL / FBX 下载与文件校验，原始结果及额外格式归档。
- 中断任务的显式恢复，身份不确定时停止监控，避免盲目重发。
- Blender 独立后台导入脚本及 19 个 MCP 工具。
- 两个真实三视图几何任务、一个真实多视图纹理任务通过。
- 20 项离线测试及 MCP 初始化、工具枚举、能力查询通过。
