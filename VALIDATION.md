# 验证范围与复现方式

日期：**2026-09-08**。本文分开记录真实官网生成、真实表单准备、离线测试和发行包检查，避免把“表单能填写”误认为“全部生成与下载已通过”。

## 真实官网生成与导出

以下结果来自 v0.2.0 开发阶段的实际工作台测试，是 v0.2.1 的功能基线。发布准备没有为了打包重复提交这些付费或扣额度任务。

| 样例 | 输入与结果 | 验证结论 |
|---|---|---|
| 陶罐几何 | 正/右/背三图，V3.1、50k 预设；结果 49,992 三角面 | 真实生成、成品绑定、STL 和 FBX 下载通过 |
| 篮筐几何 | 正/右/背三图，V3.1、50k 预设；结果 50,000 三角面 | 后台监控、完成判断和自动下载 STL 通过 |
| 陶罐纹理 | 已有几何 + 正/右/背三图；GLB 约 42.8 MB，1 个网格、1 个材质、3 张 4096×4096 贴图 | 多视图纹理生成、后台自动下载和 Blender 导入通过 |
| 自制校准模型 | 上传一个程序化测试模型，再从工作台导出 GLB | 上传与导出传输通过；此项不是 AI 生成质量测试 |

几何和纹理原件在本地保留，经过独立 Blender 进程导入、米制高度设置、资源打包和渲染检查。少量样例成功不能证明所有题材、网格、参数或账号上的成功率。

FBX 导出曾发生 Chrome 崩溃，恢复后重新选取同一个已完成资产，最终导出成功。没有为此重新生成；尚未证明所有浏览器崩溃原因已消除。

原始任务记录、账号页面、输入图、生成物和含资产 URL 的截图**不包含在发布包中**。本报告是脱敏后的测试摘要，仓库中的离线测试不能重放这些服务端生成结果。

## 真实表单准备

八个代表性表单已在当时的官网准备到可提交状态：

| 功能 | 测试内容 |
|---|---|
| 概念设计 | 文字输入与准备 |
| 几何生成 | 单图上传与参数准备 |
| 组件拆分 | 模型上传与预分割准备 |
| 低模生成 | 模型上传、低档、四边面 |
| UV 展开 | 模型上传与智能展开准备 |
| 纹理绘制 | 模型上传与文字纹理准备 |
| 绑骨蒙皮 | 模型上传与表单准备 |
| 动画生成 | 可见角色与待机模板选择 |

这些表单测试没有点击生成。绑骨表单使用的是自制校准模型，只用于测试输入和控件，**没有验证真实人物的骨骼、蒙皮或动作质量**。涉及账号和项目素材的真实网页 smoke 脚本没有作为通用测试分发。

## 离线自动化与 MCP

v0.2.1 发布准备中，`test_workbench` 和 `test_lifecycle` 共 **27 项独立测试通过**，一次执行用时约 7.3 秒。其中 20 项来自开发基线，7 项新增测试检查可分发安装相关行为。覆盖：

- 参考图检查、重复视图、输入哈希与版本去重。
- 并发提交仅一个调用取得提交资格，不确定状态拒绝盲目重发。
- 新增卡片绑定、加载占位符不误报完成、稳定缩略图恢复、身份丢失时停止。
- 对应卡片的明确失败状态、禁用控件、表单参数变更检测。
- GLB 容器、外部资源、文件签名、压缩包路径、损坏或被修改的缓存下载。
- 原始文件保留、额外导出归档、独立 Blender 导入脚本生成。
- 用户数据目录、环境变量与显式目录优先级、平台目录回退。
- 首次下载自动建立元数据目录、探测辅助函数需要显式提供有效 GLB。

stdio MCP 已完成初始化、**19 个工具**枚举和能力查询。该检查验证服务可启动及工具可见，不等于在所有 MCP 宿主中都已注册成功，也不代表每个工具都执行过真实官网生成。

在源码目录、安装依赖并装有 Chrome 的环境中运行：

```powershell
.\.venv\Scripts\python.exe -m unittest test_workbench test_lifecycle -v
.\.venv\Scripts\python.exe smoke_mcp.py
```

浏览器测试拦截请求并使用离线页面；MCP smoke 不提交生成。它们不需要混元账号，不消耗官网生成额度。测试输出应以当前执行结果为准；发布包的额外安装检查由发行记录说明。

## v0.2.1 安装包独立验证

2026-09-08，使用 Python 3.12.14 在全新虚拟环境中安装构建后的 wheel，并在源码目录之外运行测试。已确认实际导入位置为虚拟环境的 `site-packages`，没有通过源码目录或 editable 安装获得模块。

- wheel 安装成功，CLI `--help` 正常返回。
- 从独立测试目录调用已安装模块，27 项离线测试通过（8.879 秒）。
- `smoke_mcp.py --installed` 使用 Python `-I` 隔离模式启动服务，初始化、19 个工具与能力查询通过。
- 构建 wheel、源码 tar.gz，并提供源码 ZIP 与 SHA-256 校验文件。
- GitHub Actions 配置只运行离线测试与打包，不启动真实工作台、不配置混元账号。

## 仍需单独验收的功能

| 范围 | 待完成事项 |
|---|---|
| 概念设计 | 实际概念图生成、人物多视图的全部输出与下载 |
| 组件拆分 | 预分割后逐部件调整、确认、分件导出 |
| 低模 / UV | 结果质量、对应成品与下载 |
| 纹理其他模式 | 文字和单图产物；神奇笔刷的可用前提、区域与笔触流程 |
| 角色 | 合适姿态的真实人物自动绑骨、权重、关节变形 |
| 动画 | 实际生成、骨骼与动画数据、格式导出与 Blender / Unity 验收 |
| 下载 | USDZ、MP4、GIF 的完整导出 |
| 非 Studio 入口 | 普通首页图/文生 3D、全景图、世界重建；世界生成排除 |

当前发布没有“全站全功能端到端通过”的结论。网页更新后，尤其应重新检查表单定位、提交后的资产身份、完成信号及导出菜单。
